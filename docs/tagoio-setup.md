# TagoIO

1. Crie device tipo **HTTP**
2. Copie o **Token**
3. Variáveis:
   - `flow_rate` (number)
   - `total_usage` (number)
   - `leak_detected` (boolean)
   - `valve_status` (boolean)

4. Use template **Pump Control**
